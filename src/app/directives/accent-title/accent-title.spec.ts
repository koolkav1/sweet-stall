import { AccentTitle } from './accent-title';

describe('AccentTitle', () => {
  it('should create an instance', () => {
    const directive = new AccentTitle();
    expect(directive).toBeTruthy();
  });
});
