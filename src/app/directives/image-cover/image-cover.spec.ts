import { ImageCover } from './image-cover';

describe('ImageCover', () => {
  it('should create an instance', () => {
    const directive = new ImageCover();
    expect(directive).toBeTruthy();
  });
});
